<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <link rel="stylesheet" href="/stylesheets/userstyle.css" />
  </head>
  <body>
    <div class="container">
      <div class="cards">
        <% users.forEach(user => { %>
  <div class="profile-card">

    <img src="/images/<%=user.image%>" alt="person" />

    <h3><%= user.name %></h3>
    <p class="email"><%= user.email %></p>

    <p class="desc">
      <%= user.desc %>
    </p>
    <div class="ed-btns">
    <a href="/edit">Edit profile</a>
    <a href="/delete">Delete profile</a>
    </div>
  </div>
<% }) %>

      </div>
    </div>

  </body>
</html>
